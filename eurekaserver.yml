server:
  port: 8070
eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: httlp:${eureka.instance.hostname}:${server.port}/eureka/
